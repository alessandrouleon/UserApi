# UserApi

REST API built with **.NET 8**, **Clean Architecture**, **DDD**, **CQRS** (MediatR), and **SQL Server**.

## Architecture

### dotnet ef database update --project src/Infrastructure --startup-project src/Api
